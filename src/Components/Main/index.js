import React, {
  useEffect,
} from 'react'
import styled from 'styled-components'
import Home from '../Home'

function Main() {
  useEffect(() => {
    
  })

  return (
    <SContainer>
      <Home />
    </SContainer>
  )
}

export default Main

const SContainer = styled.div`
`